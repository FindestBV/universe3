import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import * as DialogStories from './Dialog.stories';

<Meta of={DialogStories} />

# Dialog

As with the current production instance, heavy use is made of Dialogs and Modal elements within the Findest Universe3 instance.
Each Dialog facilitates differing render and action triggering content. There are a number of varieties listed herein, all of whom are implemented 
using base components from ShadCN - extended in the individual variant file.  

<Canvas of={DialogStories.Default} />